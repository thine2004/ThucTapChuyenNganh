
<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800">Sửa Danh mục</h1>
</div>

<div class="card shadow mb-4">
    <div class="card-body">
        <form action="/admin/category/edit/{{category._id}}?_method=PUT" method="POST">
            <div class="form-group">
                <label for="name">Tên Danh mục</label>
                <input type="text" class="form-control" id="name" name="name" value="{{category.name}}" required>
            </div>
            <div class="form-group">
                <label for="description">Mô tả</label>
                <textarea class="form-control" id="description" name="description" rows="3">{{category.description}}</textarea>
            </div>
            <div class="form-row">
                <div class="form-group col-md-6">
                    <div class="form-check pt-4">
                        <input type="checkbox" class="form-check-input" id="isExam" name="isExam" value="true" {{#if category.isExam}}checked{{/if}}>
                        <label class="form-check-label" for="isExam">Là danh mục Luyện thi (Exams)</label>
                        <small class="form-text text-muted">Nếu chọn, danh mục này sẽ hiển thị trong phần đánh giá năng lực của học viên.</small>
                    </div>
                </div>
                <div class="form-group col-md-6">
                    <label for="maxScore">Điểm tối đa (Max Score)</label>
                    <input type="number" class="form-control" id="maxScore" name="maxScore" value="{{category.maxScore}}">
                    <small class="form-text text-muted">Ví dụ: IELTS là 9.0, TOEIC là 990.</small>
                </div>
            </div>

            <div class="form-group">
                <div class="form-check">
                    <input type="checkbox" class="form-check-input" id="isActive" name="isActive" value="true" {{#if category.isActive}}checked{{/if}}>
                    <label class="form-check-label" for="isActive">Hoạt động</label>
                </div>
            </div>
            <button type="submit" class="btn btn-primary">Cập nhật Danh mục</button>
            <a href="/admin/category" class="btn btn-secondary">Hủy</a>
        </form>
    </div>
</div>
