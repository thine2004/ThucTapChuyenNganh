<div class="container-xxl py-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="mb-4">
                    <a href="javascript:history.back()" class="btn btn-sm btn-outline-secondary">
                        <i class="fa fa-arrow-left me-2"></i>Quay lại
                    </a>
                </div>
                
                <h1 class="mb-4 text-primary">{{resource.title}}</h1>
                <div class="d-flex align-items-center mb-4 text-muted small">
                    <span class="me-3"><i class="fa fa-calendar me-2"></i>{{resource.createdAt}}</span>
                    <span><i class="fa fa-tag me-2"></i>{{resource.type}}</span>
                </div>

                <div class="card shadow-sm border-0">
                    <div class="card-body p-5">
                        <div class="resource-content">
                            {{{resource.content}}}
                        </div>
                    </div>
                </div>

                <div class="mt-5 p-4 bg-light rounded shadow-sm text-center">
                    <h5 class="mb-3">Bạn thấy kiến thức này hữu ích chứ?</h5>
                    <div class="d-flex justify-content-center gap-3">
                        <button class="btn btn-primary px-4"><i class="fa fa-thumbs-up me-2"></i>Hữu ích</button>
                        <button class="btn btn-outline-secondary px-4"><i class="fa fa-share me-2"></i>Chia sẻ</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .resource-content {
        line-height: 1.8;
        font-size: 1.1rem;
        color: #444;
    }
    .resource-content h2, .resource-content h3 {
        margin-top: 2rem;
        margin-bottom: 1rem;
        color: #2c3e50;
    }
    .resource-content img {
        max-width: 100%;
        height: auto;
        border-radius: 8px;
        margin: 1.5rem 0;
    }
</style>
